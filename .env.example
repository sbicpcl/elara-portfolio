# Copy to .env.local to enable live AI analysis.
# Without this key, Lumi runs in DEMO mode and returns clearly-labelled sample results.

# Anthropic API key (https://console.anthropic.com) — powers the vision analysis and chat.
ANTHROPIC_API_KEY=
